# Index — short-term-mem (Core)

Lazy-loaded entries in Core `short-term-mem/`. Add a one-line description per file when creating new entries.

- `active-project.md` — last-active project pointer (created on first session; committed individually and immediately)

*Empty otherwise. Entries accumulate as sessions happen.*
